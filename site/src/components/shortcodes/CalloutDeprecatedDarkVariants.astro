---
/*
 * Outputs message about dark mode component variants being deprecated in v5.3.
 */

interface Props {
  component: string
}

const { component } = Astro.props
---

<div class="bd-callout bd-callout-warning">
  <p>
    <strong>Внимание!</strong> Темные варианты для компонентов были устаревшими в v5.3.0 с введением цветовых режимов.
    Вместо добавления <code>.{component}-dark</code>, установите <code>data-bs-theme="dark"</code> на корневом элементе, родительском
    обертке или самом компоненте.
  </p>
</div>
